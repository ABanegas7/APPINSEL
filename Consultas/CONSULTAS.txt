db.insel.find();
db.insel.find({"cod":4321});
db.insel.findOne();

> upd={"$set":{"keys":["Ctl"]}};  para agregar un arreglo
{ "$set" : { "keys" : [ "Ctl" ] } }

> db.dices.update(query,upd,{"multi":1});
> var upd={"$push":{"keys":"IDD"}}; agregar un nuevo valor al arreglo

> db.dices.update(query,upd,{"multi":1}); forma para agregar un nuevo elemento al arreglo


> query = {"keys":{"$exists":1}} si existe o no

BORRAR

> query = {"docnum":100}
{ "docnum" : 100 }

> db.dices.remove(query)
WriteResult({ "nRemoved" : 1 })
>

db.dices.drop() borrar coleccion
> doc.usuario="Alex" asigna nuevo atributo "usuario":"Alex"


> db.dices.save(doc) guardar cambios

> query = {"docnum":500}
{ "docnum" : 500 }

> db.dices.find(query)

> var upd={"$set":{"usuario":"banegas"}}

> db.dices.update(query,upd) recibe filtro y valor a actualizar


> var upd={"$inc":{"views":1}}

> db.dices.update(query,upd)


 modificar todos

> db.dices.update({},upd,{"multi":1})
> db.randoms.count() contar documentos de la coleccion random

> db.randoms.find().pretty() todos los documentos organizados

> db.randoms.find({"numero":200000}) buscar solo el domumento 200000

> db.randoms.find({"mean":{$lt:1}}).pretty() $lt es menor que

> db.randoms.find({"mean":{$lt:1}}).count() contar cuantos cumplen con la condicion

> db.randoms.find({"mean":{$gt:1}}).pretty() $gt es mayor que

> db.randoms.find({"mean":{$gt:1}}).pretty() $gte es mayor o igual que


entrar a mongo

> db.randoms.find({$and:[{"mean":{$gt:50}},{"mean":{$lt:60}}]}).count() and para sacar entre dos extremos

> db.randoms.find({$or:[{"mean":{$lte:10}},{"mean":{$gte:90}}]}).count() sacar los or

cuantos doc tienen el atributo pct=50
> db.randoms.find({"pct":50}).count()
cuantos doc pct entre 0 1
> db.randoms.find({$and:[{"pct":{$gt:0}},{"pct":{$lt:1}}]}).count()

doc entre 10 o 20  extremos incluidos o mayores a 95

> db.randoms.find({$or:[{$and:[{"pct":{$gte:10}},{"pct":{$lte:20}}]},{"pct":{$gt:95}}]}).count()











> use seminario;


var vPAlabras=new Array();

vPAlabras.push("Hola");

vPAlabras.push("Adios");

vPAlabras.push("Buenos D�as");

vPAlabras.push("Buenas Noches");

vPAlabras.push("Gracias");

vPAlabras.push("Disculpe");

vPAlabras.push("Buenas Tardes");

vPAlabras.push("Bienvenido");

vPAlabras.push("Pase usted");

vPAlabras.push("No hay de que so no mas de papa");


for(var i=1;i<=100;i++){

var p1=vPAlabras[(Math.floor(Math.random()*9))],

 p2=vPAlabras[(Math.floor(Math.random()*9))],

 p3=vPAlabras[(Math.floor(Math.random()*9))],

 p4=vPAlabras[(Math.floor(Math.random()*9))],

p5=vPAlabras[(Math.floor(Math.random()*9))];


var doc={"num":i,"palabras":[p1,p2,p3,p4,p5]};

db.randomP.insert(doc);
}


1. Documentos por clasificacion

Suma Cumlaude

> db.random2.find({"Total":{$gte:95}}).count()
1

Magna Cumlaude

> db.random2.find({$and:[{"Total":{$gte:90}},{"Total":{$lt:95}}]}).count()
6

Cumlaude

> db.random2.find({$and:[{"Total":{$gte:85}},{"Total":{$lt:90}}]}).count()
10

Bene Provatus

> db.random2.find({$and:[{"Total":{$gte:70}},{"Total":{$lt:85}}]}).count()
100

Sans Provatus

> db.random2.find({"Total":{$lt:70}}).count()
884

2. Cuantos Documentos no son Reprobados

> db.random2.find({"Total":{$gte:70}}).count()
117

3. Cuantos Documentos son Cumlaude para Arriba

> db.random2.find({"Total":{$gte:85}}).count()
17



1) Cuantos documentos tiene la palabra "Hola"

> db.randomP.find({"palabras":"Hola"}).count()
41

2) Cuantos documentos tienen la palabra "Adios"

> db.randomP.find({"palabras":"Adios"}).count()
48

3) Cuantos documentos tienen las palabras "Buenos D�as" y "Gracias"

> db.randomP.find({$and:[{"palabras":"Buenos Dias","palabras":"Gracias"}]}).count()
39

4) Cuantos documentos tiene las palabras "Disculpe" y "Adios"

> db.randomP.find({$and:[{"palabras":"Disculpe","palabras":"Adios"}]}).count()
48

5) Cuantos documentos tiene las palabras "Pase Usted" o "Bienvenido"

> db.randomP.find({$or:[{"palabras":"Pase usted"},{"palabras":"Bienvenido"}]}).count()
69
